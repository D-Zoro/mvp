# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=Books4All

# FastAPI Backend
# Server-side only. Used by Next.js route handlers as the proxy target.
BACKEND_URL=http://localhost:8000
BACKEND_API_PREFIX=/api/v1

# Optional public backend URL. Keep this only if direct browser calls are needed.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Runtime
NODE_ENV=development
# Local-only: allows Next Image optimization for private/local IPs during local build testing.
NEXT_ALLOW_LOCAL_IMAGES=false

# Stripe
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_REPLACE_ME
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_ME

# Feature Flags
NEXT_PUBLIC_ENABLE_OAUTH=true
